# Environment Variables for MyPak Connect

# Enable dev mode switcher in production (set to "true" to show demo state selector)
# This allows toggling between healthy/single_urgent/multiple_urgent states in production
NEXT_PUBLIC_SHOW_DEV_TOOLS=true
